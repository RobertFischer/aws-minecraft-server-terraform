name:                server-restart-lambda
version:             0.1.0.0
github:              "RobertFischer/aws-minecraft-terraform"
license:             BSD3
author:              "Robert Fischer"
maintainer:          "smokejumperit@gmail.com"
copyright:           "(c)2021 Robert Fischer. All Rights Reserved. See LICENSE for liscensing terms."

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/RobertFischer/aws-minecraft-terraform#readme>

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src

  dependencies:
  - aeson
  - hs-aws-lambda
  - bytestring
  - amazonka
  - amazonka-ec2

executables:
  instance-up-down:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - server-restart-lambda

